trigger:
- master

pool:
  vmImage: macOS-latest

steps:
- script: |
    chmod u+x hyperexecute
    ./hyperexecute --user $(LT_USERNAME) --key $(LT_ACCESS_KEY) --config yaml/win/junit_hyperexecute_autosplit_sample.yaml --env stage
  displayName: 'run HE test'
